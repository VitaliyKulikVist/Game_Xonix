namespace Assets.Scripts.Common
{
    public enum LevelResult
    {
        None = 0,
        Win = 1,
        Lose = 2
    }

    public enum DragState
    {
        Continue,
        Canceled,
        Wait
    }
}
